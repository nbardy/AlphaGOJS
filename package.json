{
  "name": "alphagojs",
  "version": "2.0.0",
  "description": "Self-play RL training with fused WebGPU kernels — 525 games/sec on M4",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.69",
    "bun-webgpu": "^0.1.7",
    "typescript": "^5.8.0",
    "vite": "^6.0.0"
  }
}
